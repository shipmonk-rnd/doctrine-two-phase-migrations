{
    "name": "shipmonk/doctrine-two-phase-migrations",
    "description": "Two phase migrations for Doctrine ORM: before and after deploying new codebase version",
    "license": ["MIT"],
    "require": {
        "php": "8.0.*",
        "nette/finder": "^2.5",
        "doctrine/orm": "^2.10",
        "symfony/console": "^5.3"
    },
    "require-dev": {
        "editorconfig-checker/editorconfig-checker": "^10.2.3",
        "phpstan/phpstan": "^1.3.1",
        "phpstan/phpstan-phpunit": "^1.0.0",
        "phpstan/phpstan-strict-rules": "^1.1.0",
        "phpunit/phpunit": "^9.5.8",
        "slevomat/coding-standard": "^7.0.15"
    },
    "config": {
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "ShipMonk\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "ShipMonk\\": "tests/"
        }
    },
    "scripts": {
        "check": "ec src && phpcs && phpstan analyse -vvv && phpunit -vvv tests"
    }
}
